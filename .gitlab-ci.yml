pages:
  image: node:latest
  stage: deploy
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mkdir public
    - mv dist public
  artifacts:
    paths:
      - public
  only:
    - main
